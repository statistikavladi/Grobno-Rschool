{
index <- fantastico[i]
b <- gsub("[()]", "", index)
b <- gsub("[,]", "+", b)
index <- gsub("[ ]", "", b)
time[i] <-    gmapsdistance( origin = b,
destination = destinn,
mode = "walking",
shape = "long")
count <-  count + 1
}
time
print(index, destinn)
print(5, 6)
cat(5, 6)
fantastico <- coord$Fantastiko_loc[1:7]
deslat <- InitData$store_geo_latitude[1]
deslong <- InitData$store_geo_longitude[1]
destinn <- paste0(deslat, "+", deslong)
fantastico <- coord$Fantastiko_loc[1:7]
for(i in fantastico)
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
# time[i] <-    gmapsdistance( origin = b,
#                                       destination = destinn,
#                                      mode = "walking",
#                                      shape = "long")
# count <-  count + 1
cat(index, destinn)
}
fantastico <- coord$Fantastiko_loc[1:7]
for(i in fantastico)
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
# time[i] <-    gmapsdistance( origin = b,
#                                       destination = destinn,
#                                      mode = "walking",
#                                      shape = "long")
# count <-  count + 1
cat(index)
}
fantastico <- coord$Fantastiko_loc[1:7]
fantastico
for(i in fantastico)
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
# time[i] <-    gmapsdistance( origin = b,
#                                       destination = destinn,
#                                      mode = "walking",
#                                      shape = "long")
# count <-  count + 1
print(index)
}
for(i in fantastico)
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
# time[i] <-    gmapsdistance( origin = b,
#                                       destination = destinn,
#                                      mode = "walking",
#                                      shape = "long")
# count <-  count + 1
print(index)
}
fantastico
for(i in 1:length(fantastico))
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
# time[i] <-    gmapsdistance( origin = b,
#                                       destination = destinn,
#                                      mode = "walking",
#                                      shape = "long")
# count <-  count + 1
print(index)
}
fantastico <- coord$Fantastiko_loc[1:7]
count <- 1
time<-matrix(0,  ncol = 3)
time
for(i in 1:length(fantastico))
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance( origin = b,
destination = destinn,
mode = "walking",
shape = "long"))
}
time <- 0
index <- NULL
fantastico <- coord$Fantastiko_loc[1:7]
for(i in 1:length(fantastico))
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance( origin = b,
destination = destinn,
mode = "walking",
shape = "long"))
}
View(time)
time <- 0
fantastico <- coord$Fantastiko_loc[1:7]
for(i in 1:length(fantastico))
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
# time <-cbind(time, gmapsdistance( origin = b,
#                                       destination = destinn,
#                                      mode = "walking",
#                                      shape = "long"))
print(index)
}
coord$X <- NULL
time <- 0
index <- NULL
fantastico <- coord$Fantastiko_loc[1:7]
for(i in 1:length(fantastico))
{
index <- fantastico[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
View(time)
time
coord$X <- NULL
time <- NULL
index <- NULL
rm(list = ls())
InitData <- read.xlsx("Downloads/Dataton/Kaufland/kaufland_20170322_data_datathon.xlsx", sheet = 2 )
coord <- read.csv("Downloads/Dataton/Kaufland/competitors_locations.csv")
set.api.key("AIzaSyDYYosd6Lt2tU8_15bzkXDbmyeg3x0SIqA")
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$Fantastiko_loc
tempStore
tempStore
index(tempStore)
str(tempStore[11])
deslat <- InitData$store_geo_latitude[1]
deslong <- InitData$store_geo_longitude[1]
destinn <- paste0(deslat, "+", deslong)
for(i in 1:length(fantastico))
{
index <- tempStore[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
for(i in 1:length(tempStore))
{
index <- tempStore[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
coord$X <- NULL
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$Fantastiko_loc
for(i in 1:length(tempStore))
{
if( i == NULL ) break
index <- tempStore[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
i
tempStore <- coord$Fantastiko_loc
for(i in 1:length(tempStore))
{
if( i == NULL ) break
index <- tempStore[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
i
length(tempStore)
length(tempStore)
time <- NULL
is.null(time)
apply(tempStore, 1, function(x) is.null(x))
?apply
apply(tempStore, 1, is.null(x))
apply(tempStore, 1, is.null(tempStore))
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$Fantastiko_loc
tempStore
View(coord)
is.null(tempStore)
a <- ifelse()
?ifelse
a <- ifelse(is.null(tempStore), "")
a <- ifelse(is.null(tempStore), 1)
a <- ifelse(is.null(tempStore), 1, 0)
a
a <- ifelse(is.null(tempStore), "", tempStore)
a
a <- apply(tempStore, 1, function(x) ifelse(is.null(x), "", x))
a <- apply(tempStore, 1, function(x) ifelse(is.null(x), 1, x))
a <- apply(tempStore, function(x) ifelse(is.null(x), 1, x))
n <- ifelse(is.null(tempStore), "", 2)
n
n <- ifelse(is.null(tempStore), "", tempStore)
n
tempStore
tempStore <- coord$T.market_loc
tempStore
n <- ifelse(is.null(tempStore), "", tempStore)
n
n <- ifelse(is.null(tempStore), "", tempStore)
n
tempStore <- coord$T.market_loc
tempStore <- coord$BILLA_loc
tempStore
n <- ifelse(is.null(tempStore), 0, 1)
n
tempStore
is.null(tempStore)
length(tempStore)
tempStore <- coord$Lidl_loc
tempStore
tempStore <- coord$Fantastiko_loc
tempStore
length(tempStore)
length(!tempStore)
length(is.null(tempStore))
sum(tempStore)
as.numeric(tempStore)
tempStore
n <- as.numeric(tempStore)
n
n <- n[n != 1]
n
length(n)
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$Fantastiko_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:length(n))
{
index <- tempStore[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
View(time)
View(time)
coord$X <- NULL
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$T.market_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:length(n))
{
index <- tempStore[i]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
View(time)
for(j in 1:length(n))
{
index <- tempStore[j]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
View(InitData)
View(coord)
View(InitData)
InitData <- subset(InitData, store_town == "София")
View(InitData)
ncol(InitData)
View(coord)
View(InitData)
nrow(InitData)
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$T.market_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:nrow(InitData)){
for(j in 1:length(n))
{
deslat <- InitData$store_geo_latitude[i]
deslong <- InitData$store_geo_longitude[i]
destinn <- paste0(deslat, "+", deslong)
index <- tempStore[j]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
}
View(time)
colnames(time)
rownames(time)
names(time)
View(time)
time <- as.data.frame(time)
time
View(time)
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$Fantastiko_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:nrow(InitData)){
for(j in 1:length(n))
{
deslat <- InitData$store_geo_latitude[i]
deslong <- InitData$store_geo_longitude[i]
destinn <- paste0(deslat, "+", deslong)
index <- tempStore[j]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
tempStore <- coord$Fantastiko_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:nrow(InitData)){
for(j in 1:length(n))
{
deslat <- InitData$store_geo_latitude[i]
deslong <- InitData$store_geo_longitude[i]
destinn <- paste0(deslat, "+", deslong)
index <- tempStore[j]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
tempStore <- coord$Fantastiko_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:nrow(InitData)){
for(j in 1:length(n))
{
deslat <- InitData$store_geo_latitude[i]
deslong <- InitData$store_geo_longitude[i]
destinn <- paste0(deslat, "+", deslong)
index <- tempStore[j]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
}
coord$X <- NULL
time <- NULL
index <- NULL
tempStore <- coord$Fantastiko_loc
n <- as.numeric(tempStore)
n <- n[n != 1]
for(i in 1:nrow(InitData)){
for(j in 1:length(n))
{
deslat <- InitData$store_geo_latitude[i]
deslong <- InitData$store_geo_longitude[i]
destinn <- paste0(deslat, "+", deslong)
index <- tempStore[j]
index <- gsub("[()]", "", index)
index <- gsub("[,]", "+", index)
index <- gsub("[ ]", "", index)
time <-cbind(time, gmapsdistance(origin = index,
destination = destinn,
mode = "walking",
shape = "long"))
print(index)
}
}
View(time)
colnames(time)
install.packages("Rcmdr")
library(Rcmdr)
library(Rcmdr)
setwd("Documents/R/Grodno")
setwd("Documents/Grodno/")
setwd("Documents")
setwd("Documents/Grodno")
setwd("Documents/Grodno/")
getwd()
setwd("/home/vladi-fisher/Documents/Grodno/)"
\
getwd
getwd()
rm(list = ls())
test <- "That's Works !"
?ln
?log
ln <- (10, base = 2)
ln <- log(10,  base = 2)
ln
?log
?pi
pi
area <- pi * cylenderRadius ^ 2 * cylenderHeight
cylenderRadius <- 1.5
cylenderHeight <- 2
area <- pi * cylenderRadius ^ 2 * cylenderHeight
area
rm(list = ls())
cylenderRadius <- 1.5
cylenderHeight <- 2
volume <- pi * cylenderRadius ^ 2 * cylenderHeight
cat(volume)
cat("Volume  is ", volume)
cylenderRadius <- 1.5
cylenderHeight <- 2
volume <- pi * cylenderRadius ^ 2 * cylenderHeight
install.packages("RCurl")
library(RCurl)
library(RCurl)
page <- read_html("https://en.wikipedia.org/wiki/List_of_largest_employers_in_the_United_States")
employers <- page %>%
html_nodes("table") %>%
.[1] %>%
html_table()
library(RCurl)
install.packages("jsonlite")
